import styled from "styled-components";

export const CollapsideParkingTitle = styled.main`
    width: 100%;
    margin: 0 auto;
    max-width: 700px;
    height: 50px; 
    background-color: var(--grey);
    border-radius: 8px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 20px;
    font-weight: bold;
    color: var(--blue);
    padding: 0 20px;
`;